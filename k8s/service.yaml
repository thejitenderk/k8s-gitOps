apiVersion: v1
kind: Service
metadata:
  name: APP_NAME-svc
  namespace: NAMESPACE
  labels:
    app: APP_NAME
spec:
  type: LoadBalancer
  selector:
    app: APP_NAME
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000

apiVersion: v1
kind: Service
metadata:
  name: react-app-svc
  namespace: staging
  labels:
    app: react-app
spec:
  type: NodePort
  selector:
    app: react-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080